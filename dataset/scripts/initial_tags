LOAD CSV WITH HEADERS FROM "https://github.com/nhpquy/database-final-project/blob/master/dataset/tags.csv" AS row
WITH row
MATCH (product:Product {id: toInteger(row.productId)})
MERGE (tag:Tag {tag: row.tag})
MERGE (product)-[r:HAS_TAG]->(tag)