LOAD CSV WITH HEADERS FROM "https://raw.githubusercontent.com/nhpquy/database-final-project/master/dataset/tags.csv" AS row
WITH row
MATCH (product:Product {id: toInteger(row.productId)})
MERGE (tag:Tag {tag: row.tag})
MERGE (product)-[r:HAS_TAG]->(tag)